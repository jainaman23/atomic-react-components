import { action } from '@storybook/addon-actions';

const defaultConfig = {
  slideTransitionTime: action('number'),
  title: 'Spotlight',
  nav: false,
  width: '200px',
  height: '200px',
  slideAnimationTime: 2000,
};

export default defaultConfig;
